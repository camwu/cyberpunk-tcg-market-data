name: Daily Market Price Scrape

on:
  schedule:
    # 20:17 UTC daily (1:17 PM PDT / 4:17 PM EDT)
    - cron: '17 20 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: 'Force re-scrape even if today snapshot exists'
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.DATA_SYNC_TOKEN }}
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Run Scraper
        env:
          FORCE_SCRAPE: ${{ inputs.force }}
        run: |
          if [ "$FORCE_SCRAPE" = "true" ]; then
            python scrape.py prices --force
          else
            python scrape.py prices
          fi

      - name: Commit and push if prices updated
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add prices/ cards.json
          if git diff --staged --quiet; then
            echo "No price or catalog changes to commit."
          else
            git commit -m "Auto-update market prices & catalog: $(date -u +'%Y-%m-%d')"
            git push origin main
          fi
